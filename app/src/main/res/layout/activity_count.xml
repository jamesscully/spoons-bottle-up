<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/count_layout_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.CountActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/count_toolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:theme="?attr/actionBarTheme"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:title="Overview" />

    <ScrollView
        android:id="@+id/count_layout_scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/count_toolbar">

        <LinearLayout
            android:id="@+id/count_layout_main"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical" >

            <Space
                android:layout_width="match_parent"
                android:layout_height="64dp" />
        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/count_button_bottleup"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_marginStart="160dp"
        android:layout_marginEnd="163dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/button_bottleup"
        android:text="Bottle Up!  🍻"
        android:textColor="#ffffff"
        app:layout_constraintBottom_toBottomOf="@+id/count_layout_scroll"
        app:layout_constraintEnd_toEndOf="@+id/count_layout_scroll"
        app:layout_constraintStart_toStartOf="@+id/count_layout_scroll" />

</androidx.constraintlayout.widget.ConstraintLayout>